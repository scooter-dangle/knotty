{
    "$schema": "https://json.schemastore.org/claude-code-settings.json",
    "permissions": {
        "allow": [
            "Bash(git fetch)",
            "Bash(git fetch *)",
            "Bash(git pull --rebase)",
            "Bash(git pull --rebase *)"
        ]
    }
}
