# This example shows a vertical Reidemeister II opportunity
(0
(1

# The following two lines can be removed.
# Technically, we also end up swapping the first two lines, but with the
# index adjustments, they end up being exactly the same.
\0
/2

# A(N) followed immediately by V(N + 1) or V(N - 1) can be dropped
(2
)3

)1
)0
